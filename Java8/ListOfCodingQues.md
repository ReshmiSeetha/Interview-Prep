Java Stream API – Coding Questions Only
🟢 Basic Level
Convert a list of strings to uppercase using Stream API.
Find all even numbers from a list of integers.
Count the number of elements in a list using streams.
Find the first element in a list using streams.
Remove duplicate elements from a list.
Sort a list of integers in ascending order.
Sort a list of strings in descending order.
Convert a list of integers into a comma-separated string.
Check if all elements in a list are greater than 10.
Find any element in a list that starts with "A".
🟡 Intermediate Level
Find the maximum number in a list.
Find the minimum number in a list.
Find the sum of all elements in a list.
Find the average of numbers in a list.
Find the second highest number in a list.
Find all numbers greater than 50 and sort them.
Group a list of strings by their length.
Count occurrences of each element in a list.
Find duplicate elements in a list.
Find the longest string in a list.
Merge two lists and remove duplicates.
Partition numbers into even and odd.
Convert a list of objects into a map (key → value).
Flatten a list of lists into a single list.
Filter employees whose salary is greater than 50k.
🔴 Advanced Level
Find top 3 highest numbers in a list.
Find the first non-repeated character in a string using streams.
Find the frequency of each character in a string.
Find the longest word in a sentence.
Find all palindromes in a list of strings.
Sort a list of objects based on multiple fields (e.g., salary then name).
Group employees by department and count them.
Find the highest paid employee in each department.
Convert a list of strings into a map of word → length.
Find common elements between two lists.
Find elements that appear only once in a list.
Reverse each string in a list using streams.
Find the sum of squares of all even numbers.
Find the kth smallest element in a list.
Find the longest consecutive sequence in a list (stream-based approach).
🔥 Real Interview / Scenario-Based
Given a list of transactions, find total amount per user.
From a list of employees, find those who joined after 2020.
Find duplicate words in a paragraph.
Given a list of orders, group them by status and count.
Find the most frequent element in a list.
Find employees whose names start with a vowel.
Convert a list of objects into nested map (dept → employees).
Find all anagrams in a list of strings.
Given a list of integers, separate prime and non-prime numbers.
Find the first 5 even numbers after squaring and sorting.